# Boarding Frontend

Minimal React + Tailwind scaffold.

Quick start (Windows PowerShell):

```
cd boarding-frontend
npm install
npm start
```

Set the backend API base via environment variable:

```
# Windows PowerShell
$env:REACT_APP_API_BASE = "http://localhost:8000/api"
npm start
```
